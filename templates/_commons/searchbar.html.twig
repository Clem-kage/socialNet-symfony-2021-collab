<div class="container">
    {{ form_start(searchForm) }}
    <div class="d-flex">
        <div class="w-100">
            {{ form_row(searchForm.word) }}
        </div>
        <div class="d-flex align-items-end">
            {{ form_row(searchForm.submit) }}
        </div>
    </div>
    {{ form_end(searchForm) }}
</div>